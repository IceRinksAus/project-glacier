{
  "name": "project-glacier",
  "version": "0.1.0",
  "private": true,
  "description": "The operating system for seasonal attractions",
  "scripts": {
    "dev": "Start development",
    "build": "npm --prefix apps/api run build && NEXT_DIST_DIR=.next-release npm --prefix apps/web run build -- --webpack",
    "test": "npm --prefix apps/api test -- --runInBand && npm --prefix apps/web test",
    "verify:release": "bash scripts/verify-release.sh"
  }
}
